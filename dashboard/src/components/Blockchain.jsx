import React from 'react';
import { Container, Typography } from '@mui/material';

export default function Blockchain() {
    return (
        <Container>
            <Typography variant="h3">Blockchain Ledger</Typography>
            <Typography>Blockchain visualization will be displayed here.</Typography>
        </Container>
    );
}
